// Transofrm raw filters (from "filters.json") to a better types and apply some changes if needed

import type { RawDataStructureDefinition, TransformedFilters } from "./types";

// Side note: acá podemos activar o desactviar, cambiar el orden, etc. de los filtros.
export function transformFilters(rawDataStructureDefinition: RawDataStructureDefinition): TransformedFilters {
    // TODO: transform data and return ...
}